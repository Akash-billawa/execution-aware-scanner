apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: execution-aware-scanner

namespace: kube-system

resources:
  - daemonset.yaml
  - service.yaml
  - clusterrole.yaml
  - clusterrolebinding.yaml

images:
  - name: scanner
    newName: ghcr.io/akash-billawa/execution-aware-scanner
    newTag: main
